create table first_mahogany (
    id        INTEGER          NOT NULL,
    CONSTRAINT first_mahogany_pk PRIMARY KEY(id)
);

create table second_mahogany (
    id        INTEGER          NOT NULL,
    CONSTRAINT second_mahogany_pk PRIMARY KEY(id)
);